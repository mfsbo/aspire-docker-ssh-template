/**
 * Date utilities for shared use across all apps.
 *
 * Import via alias (configured in each app's vite config / tsconfig):
 *   import { formatBuildDate, getBuildYear } from '@aspire-template/shared-utils/Date'
 */
export { formatBuildDate } from './formatBuildDate.js'
export { getBuildYear } from './getBuildYear.js'
